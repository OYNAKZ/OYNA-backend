# Project API

Minimal FastAPI project scaffold for clubs, branches, zones, seats, reservations, and sessions.

## Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Test

```bash
pytest app/tests
```
